# Observability

Use the **Ops** dashboard (`/ops`, requires `system.debug.view`) for health, readiness, and metrics JSON snapshots.

During pilot, admins should correlate spikes in errors with audit logs and user feedback (`/pilot/metrics`).
