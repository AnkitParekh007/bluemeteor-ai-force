# Angular production build already uses `src/environments/environment.prod.ts`
# with agentApiBaseUrl: '/api'. No build-time env file is required when serving
# behind nginx with `/api` → backend proxy. Customize only if you use a separate API host.

# Example override (not read by default CLI build):
# NG_APP_API_BASE_URL=https://api.internal.example.com
